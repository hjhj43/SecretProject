<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="secretproject.user.serviceimpl.UserMapper">

	<!-- 회원 목록 조회 -->
	<select id="selectUserList" parameterType="userVO" resultType="userVO">
		SELECT USER_ID, USER_NAME, USER_EMAIL, USER_PHONE
		FROM user
	</select>
	
	<!-- 회원정보 상세보기 -->
	<select id="selectDetail" parameterType="String" resultType="userVO">
		SELECT USER_ID, USER_PW, USER_NAME, USER_EMAIL, USER_PHONE
		FROM user
		WHERE USER_ID = #{userId}
	</select>
	
	<!-- 회원가입 -->
	<insert id="insertUser" parameterType="userVO">
		INSERT INTO user(USER_ID, USER_PW, USER_NAME, USER_EMAIL, USER_PHONE)
		VALUES(#{userId},#{userPw},#{userName},#{userEmail},#{userPhone})
	</insert>
	
	<!-- 아이디 중복 체크 -->
	<select id="idCheck" parameterType="userVO" resultType="int">
		SELECT COUNT(*) FROM user
		WHERE USER_ID = #{userId}
	</select>
	
	<!-- 로그인 -->
	<select id="getUserData" resultType="userVO">
		SELECT USER_ID, USER_PW
		FROM user
		WHERE USER_ID = #{userId}
	</select>
	
	<!-- 회원정보 수정 -->
	<update id="updateUser" parameterType="userVO">
		UPDATE user SET
		USER_PW = #{userPw}, USER_NAME = #{userName}, 
		USER_EMAIL = #{userEmail}, USER_PHONE = #{userPhone}
		WHERE USER_ID = #{userId}
	</update>
	
	<!-- 회원정보 삭제 -->
	<delete id="deleteUser" parameterType="String">
		DELETE FROM user
		WHERE USER_ID = #{userId}
	</delete>
	
</mapper>